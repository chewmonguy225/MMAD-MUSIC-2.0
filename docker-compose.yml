version: "3.9"

services:
  backend:
    build: ./backend
    container_name: mmad-backend
    ports:
      - "8080:8080"
    environment:
      SPRING_DATASOURCE_URL: jdbc:mysql://host.docker.internal:3306/mmad
      SPRING_DATASOURCE_USERNAME: root
      SPRING_DATASOURCE_PASSWORD: 045816SQL
    networks:
      - app-network

networks:
  app-network:
    driver: bridge